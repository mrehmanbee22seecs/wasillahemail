# Next.js Environment Variables
# Copy this file to .env.local and fill in your values
# Note: API keys should NOT be exposed to the frontend in production
# Use Firebase Functions for secure email sending

# Firebase Configuration (already set in src/config/firebase.ts)
# No Resend API keys needed here - email sending happens via Firebase Functions